# PR Notes

## Outstanding from Current Request
- GUI dependency bundling for offline installs, removal of simulated metric fallbacks, terminology alignment (ρ/τ/ζ/ζ_phase/κ), PYTHONPATH auto-inclusion for vendored networkx, and loop extent calibration defaults are still pending.
- Confirm whether demo screenshots need to be regenerated now that Phase 1 shows topology descriptors beneath each substrate heatmap.
