# Phase 51 — Sixth Positive Noisy Scaffold Benchmark

This phase adds **benchmark K (hub-weave)** as a sixth structurally different positive noisy scaffold benchmark.

The benchmark is evaluated under the **unchanged pooled-five positive noisy scaffold rule**. No benchmark-K-specific coefficient refit is used.

At the switch slice `γ = 0.30`, benchmark K gives:
- train `R² ≈ 0.9648`
- held-out base `R² ≈ 0.9728`
- held-out new `R² ≈ 0.9537`
- held-out combined `R² ≈ 0.9611`
