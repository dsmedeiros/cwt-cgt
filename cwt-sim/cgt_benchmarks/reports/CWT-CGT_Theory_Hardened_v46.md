# CWT-CGT Theory Hardened v46

The theory remains layered. The coherent, branch-resolved passive core remains the strongest layer.

This revision adds a sixth structurally different positive noisy scaffold benchmark, **K (hub-weave)**, evaluated under the unchanged pooled-five noisy scaffold rule. At the switch slice `γ = 0.30`, benchmark K gives train `R² ≈ 0.9648`, held-out base `R² ≈ 0.9728`, held-out new `R² ≈ 0.9537`, and held-out combined `R² ≈ 0.9611`.

The same unchanged pooled-five rule also survives an **even harder perturbation family** on K, with held-out extreme-family `R² ≈ 0.9132` and held-out combined `R² ≈ 0.9302` at the switch slice.

This further strengthens the noisy scaffold layer as a shared scaffold-level rule, while still not constituting external empirical validation or a final microscopic open-system theory.
