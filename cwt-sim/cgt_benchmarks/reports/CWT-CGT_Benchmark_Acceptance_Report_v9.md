# CWT-CGT Benchmark Acceptance Report — v9

## Accepted benchmark roles

- **benchmark_a**: accepted null-like control.
- **benchmark_b**: accepted weak-control benchmark.
- **benchmark_c**: accepted positive coherent benchmark and accepted positive noisy benchmark; now also supported by a generator-derived tangent sign-boundary predictor.
- **benchmark_d**: accepted null-like control.
- **benchmark_f**: accepted explicit R4 / exclusion benchmark.
- **benchmark_e_topology**: accepted auxiliary periodic/gapped topology benchmark only.

## New Phase 15 acceptance consequence

Benchmark C now clears a stronger noisy standard than before: the generator-derived centered tangent field aligns with the empirical noisy local field on `25` overlapping valid cells with correlation `0.9805868137936634`, affine-fit `R² = 0.9615504993860086`, and sign agreement `1.0`.

## Current project reading

The project is now strongest as:

1. a coherent branch-resolved geometric-response core,
2. a separate R4 branch-switching lane,
3. an auxiliary topology lane,
4. an empirical noisy local field,
5. and a generator-derived tangent predictor for that noisy field.
