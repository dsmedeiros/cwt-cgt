# Theory v47

The theory remains strongest as a layered, benchmarked research framework.

Latest scaffold extension:
- added **benchmark L (fork-mesh)** as a seventh structurally different positive noisy scaffold benchmark under the unchanged pooled-five noisy rule
- added an **adversarial sign-break family** on L under that same unchanged rule

Switch-slice highlights at `γ = 0.30`:
- benchmark L held-out combined `R² ≈ 0.9744` with sign agreement `= 1.0`
- adversarial family combined `R² ≈ -0.0874` with sign agreement `≈ 0.9074`

Interpretation:
The fixed pooled-five noisy scaffold rule still transfers to a seventh positive benchmark, but the adversarial family partially breaks sign robustness. That is useful because it exposes a real boundary rather than merely pushing `R²` down.
