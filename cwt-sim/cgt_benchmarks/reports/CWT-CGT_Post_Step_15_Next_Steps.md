# CWT-CGT Post-Step-15 Next Steps

## Highest-value next move

Replace the current finite ordered-transport tangent construction with a more explicit **generator Jacobian / superoperator tangent** derivation, so the structural field is tied to differential generator data rather than only finite ordered steps.

## After that

1. test the tangent field on one additional positive benchmark variant rather than only benchmark C;
2. compare the centered tangent field against the empirical field on denser dephasing grids;
3. decide whether the old Phase 13 patch atlas should remain only as a deprecated archival report;
4. tighten the benchmark-control interpretation for weak baseline tangent structure in null-like controls.
