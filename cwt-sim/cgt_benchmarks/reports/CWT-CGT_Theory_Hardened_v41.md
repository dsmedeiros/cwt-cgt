# CWT-CGT Theory (Hardened) v41

## Core status
The coherent, branch-resolved passive core remains the strongest accepted layer of the framework.

The noisy extension now has support across **three positive scaffold benchmarks** under a shared pooled rule:
- benchmark C
- benchmark G
- benchmark H

## What Phase 42 adds
Phase 42 adds a third positive noisy scaffold benchmark, **benchmark H**, and evaluates it with the accepted pooled positive-noisy scaffold rule from Phase 41 **without benchmark-specific refit**.

This strengthens the noisy lane from:
- one positive benchmark with transfer,
- to two positive scaffold benchmarks with a pooled scaffold rule,
- to **three positive scaffold benchmarks**, where the third benchmark still transfers under the unchanged pooled rule.

## Accepted interpretation
The noisy scaffold rule is now best described as a **shared scaffold-level structured rule** rather than a single-benchmark patch. The framework still does **not** claim shape universality or external empirical validation.

## Boundary conditions
- This is still scaffold evidence, not external validation.
- The noisy lane remains less mature than the coherent core.
- The current generator/open-system construction is still a scaffold microscopic model, not a final derivation.

## Phase 42 switch-slice result
At `γ = 0.30`, benchmark H shows:
- train `R² ≈ 0.9648`
- held-out base `R² ≈ 0.9761`
- held-out new `R² ≈ 0.9604`
- held-out combined `R² ≈ 0.9667`
- combined correlation `≈ 0.9986`
- sign agreement `= 1.0`

## Current scaffold verdicts
- A: null_like
- B: weak_control
- C: local_superoperator_compactness_normalizer_supported
- D: null_like
- F: excluded_R4
- G: second_positive_noisy_scaffold_supported
- H: third_positive_noisy_scaffold_supported
