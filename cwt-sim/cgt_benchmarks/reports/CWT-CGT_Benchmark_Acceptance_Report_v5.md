# CWT-CGT Benchmark Acceptance Report v5

## Passive / coherent benchmark block

- benchmark A: pass as null-like control.
- benchmark B: pass as weak / patchy line benchmark with R4 upper band.
- benchmark C: pass as positive signed-loop benchmark.
- benchmark D: pass as null-like random-walk control.
- benchmark F: pass as explicit R4 / exclusion benchmark.

## Auxiliary topology block

- benchmark E: pass as periodic, gapped auxiliary topology benchmark.

## Mixed-state / noisy block after Phase 11

- benchmark A: no trusted mixed-state loop law at the current switch point.
- benchmark B: no strong trusted mixed-state loop law at the current switch point.
- benchmark C: trusted mixed-state loop law survives the Lindblad-style generator upgrade with R² ≈ 0.8610126798341613.
- benchmark D: no trusted mixed-state loop law at the current switch point.
- benchmark F: mixed-state loop fits remain excluded by R4 switching.

## Current acceptance judgment

The project now supports a layered claim:

1. passive coherent geometry works on the trusted branch-resolved benchmarks;
2. legitimate topology is confined to the separate periodic/gapped auxiliary sector;
3. the mixed-state positive benchmark survives a stronger continuous-time noisy implementation;
4. explicit branch-switching benchmarks remain exclusions rather than false positives.
